{
  "dashboard": {
    "id": null,
    "uid": "provider-latency",
    "title": "Provider Latency",
    "schemaVersion": 37,
    "version": 1,
    "refresh": "15s",
    "panels": [
      {
        "type": "timeseries",
        "title": "Provider Latency (p50/p95)",
        "gridPos": { "h": 8, "w": 24, "x": 0, "y": 0 },
        "targets": [
          { "expr": "histogram_quantile(0.5, sum(rate(provider_latency_seconds_bucket[5m])) by (le, provider))", "refId": "A" },
          { "expr": "histogram_quantile(0.95, sum(rate(provider_latency_seconds_bucket[5m])) by (le, provider))", "refId": "B" }
        ]
      }
    ]
  },
  "overwrite": false
}
