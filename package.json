{
  "name": "multi-agent-platform",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "backend": "node backend/server.js",
    "build": "vite build && node scripts/write-build-index.mjs dist",
    "build:docs": "vite build --outDir docs && node scripts/write-build-index.mjs docs && node scripts/sync-root-assets.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
